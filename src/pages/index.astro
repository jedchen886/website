---
// Root page redirects to Chinese version by default
// This site uses i18n with prefixDefaultLocale: true, so / is the entry point
// and /cn/ is the actual default locale home
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DORA Robotics - 机器人朋友</title>
    <meta http-equiv="refresh" content="0;url=/cn" />
    <script>
      // Immediate JavaScript redirect for better UX
      window.location.replace('/cn');
    </script>
    <style>
      /* Minimal styles for the redirect page */
      body {
        margin: 0;
        padding: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: system-ui, -apple-system, sans-serif;
        background: linear-gradient(135deg, rgb(43, 116, 255), rgb(0, 200, 150));
      }
      .redirect-container {
        text-align: center;
        color: white;
        padding: 2rem;
      }
      .redirect-container h1 {
        margin: 0 0 1rem 0;
        font-size: 1.5rem;
      }
      .redirect-container p {
        margin: 0 0 1.5rem 0;
        opacity: 0.9;
      }
      .redirect-container a {
        display: inline-block;
        padding: 0.75rem 1.5rem;
        margin: 0.25rem;
        border-radius: 9999px;
        background: rgba(255, 255, 255, 0.2);
        color: white;
        text-decoration: none;
        transition: all 0.2s ease;
        border: 1px solid rgba(255, 255, 255, 0.3);
      }
      .redirect-container a:hover {
        background: rgba(255, 255, 255, 0.3);
        transform: translateY(-2px);
      }
    </style>
  </head>
  <body>
    <div class="redirect-container">
      <h1>欢迎访问 DORA Robotics</h1>
      <p>正在跳转至中文版...</p>
      <noscript>
        <p>请选择您的语言 / Please select your language:</p>
        <a href="/cn">中文</a>
        <a href="/en">English</a>
      </noscript>
    </div>
  </body>
</html>
