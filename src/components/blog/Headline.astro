---
const { title = await Astro.slots.render('default'), subtitle = await Astro.slots.render('subtitle') } = Astro.props;
---

<header class="mb-4 md:mb-12 text-center max-w-3xl mx-auto">
  <h1 class="text-2xl md:text-3xl font-bold leading-snug tracking-tight font-heading" set:html={title} />
  {
    subtitle && (
      <div class="mt-2 md:mt-3 mx-auto text-base text-gray-500 dark:text-slate-400 font-medium" set:html={subtitle} />
    )
  }
</header>
